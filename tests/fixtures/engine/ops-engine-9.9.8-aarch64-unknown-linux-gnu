#!/bin/sh
# Pretend ops-engine 9.9.8 for aarch64-unknown-linux-gnu — a tests/fixtures/engine
# stand-in, not a real build. Answers only the one subcommand
# engine::smoke::probe_version invokes.
[ "$1" = "version" ] || exit 64
printf '%s\n' '{"protocolVersion":1,"operation":"version","ok":true,"result":{"engineVersion":"9.9.8","protocolVersion":1,"build":{"targetOs":"linux","targetArchitecture":"aarch64","gitCommit":null}},"warnings":[],"error":null}'
